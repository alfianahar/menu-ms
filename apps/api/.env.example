# Port for the API server (optional, defaults to 8000 in development)
PORT=8000

# Set the environment (development/production)
NODE_ENV=development

# CORS configuration
CORS_ORIGIN_DEVELOPMENT=http://localhost:3000
CORS_ORIGIN_PRODUCTION=https://your-production-domain.com
